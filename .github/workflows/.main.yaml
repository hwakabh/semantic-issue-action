name: Test Actions

on:
  pull_requests:
  workflow_dispatch:

jobs:
  test-action:
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v4

      - uses: hwakabh/semantic-issue-action@v0.0.1
        with:
          repo: 'hwakabh/semantic-issue-action'
