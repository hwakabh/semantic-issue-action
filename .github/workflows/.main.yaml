name: Test semantic-issue-action

on:
  push:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v4

      - uses: hwakabh/semantic-issue-action@v0.1.0
        with:
          repo: 'hwakabh/semantic-issue-action'
